import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './app.jsx'

// TODO: add path routing although we only have one route.
ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
